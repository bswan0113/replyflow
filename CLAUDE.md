# 소설 작업 통합 하네스

이 저장소는 웹소설 창작의 전 과정(아이디어→평가→세계관/플롯→캐릭터→플랫폼 선정→집필→게시 필수요소→게시→관리→피드백)을 지원하는 개인용 로컬 하네스다. 사용자 1인이 로컬에서 Claude Code와 Codex CLI를 단계별로 자유롭게 번갈아 사용한다. API 키 연동은 없으며, 두 CLI 자체의 로그인/실행 환경을 그대로 사용한다.

## 도메인 파이프라인

| # | 단계 | 담당 | 산출물 | 상태 |
|---|------|------|--------|------|
| 0 | 하네스 기반구조 | - | 스켈레톤/스키마/컨벤션/`harness-reviewer` | 완료 |
| 1 | 아이디어 구축 | Claude(대화형) + `idea-evaluator` | 소재 1차평가 + 시놉시스 1편 + 플랫폼/분량/사업성 가결정 + 최종평가 | 완료 |
| 2 | 평가 | 1단계 프로세스에 통합 — 별도 산출물 없음 | (1단계의 1차평가·최종평가로 흡수됨) | - |
| 3 | 세계관·플롯 | 자유형식 초기설계 + `worldbuilder-update`/`plot-architect-update`(확장, 언제든 호출 가능) | 세계관 문서 + 전체 아크·회차별 비트(최소 10화) + 변경 changelog | 완료 |
| 4 | 캐릭터 생성 | 자유형식 초기설계 + `character-designer-update`(확장, 언제든 호출 가능) | 캐릭터 프로필 + 관계도 + 변경 changelog | 완료 |
| 5 | 플랫폼 선정 | 자동 재검토(`platform-scout`) + 단일 go/no-go | 1단계 가결정 확정(`platform.confirmed`/`verified`) + `platform-notes.md` | 완료 |
| 6 | 집필 | 집필(단일 세션, 서브에이전트 위임 없음) + 필요시 `worldbuilder-update`/`plot-architect-update`/`character-designer-update` | 회차 원고 + 누적 요약(`summaries.md`, 떡밥 추적 표 포함) | 진행 중 |
| 7 | 게시 필수요소 | `publish-assistant` | 소개글/태그/표지문구 | 미착수 |
| 8 | 게시 | `publish-assistant` + 로컬 브라우저 | 게시 체크리스트 실행 | 미착수 |
| 9 | 관리 | (스킬) | `stats.md` | 미착수 |
| 10 | 피드백 | (스킬) | 피드백 로그, 1·3단계 순환 | 미착수 |

각 단계의 상세 스펙은 `docs/phases/NN-*.md`에 있다. 아직 만들어지지 않은 단계는 파일이 없다.

"상태" 컬럼은 **하네스 메타 빌드 진행 상황**을 가리킨다(그 단계의 절차·서브에이전트·스킬이 설계·구현·완성도 재평가까지 끝났는지). 실제 소설 프로젝트가 그 단계를 몇 번째 밟고 있는지는 각 프로젝트의 `project.yaml`의 `status`가 가리킨다 — 서로 다른 것을 추적하는 별개의 상태값이다.

## 메타 빌드 루프

이 하네스 자체를 만들어가는 절차. 하네스 사용(집필 등)이 아니라 **하네스를 확장할 때** 적용한다.

1. 지금이 몇 단계인지 제안한다.
2. 사용자가 그 단계의 작업방식(원하는 진행 스타일)을 설명한다.
3. `harness-reviewer` 서브에이전트로 그 작업방식의 합리성/생산성을 평가한다.
4. 통과하면 설계문서 초안을 작성해 제시하고, 미통과면 개선안을 반영해 재설계 후 다시 제시한다.
5. 사용자가 최종 승인한다.
6. 실제 구현(파일 생성)을 진행한다.
7. 완성 후 `harness-reviewer`로 완성도를 재평가한다.
8. 통과하면 다음 단계로 넘어가고, 아니면 보완 후 재평가한다.

결정 내역은 `docs/design-log.md`에 단계별로 기록한다.

## 컨벤션

자세한 내용은 `docs/phases/00-foundation.md` 참고. 요약:

- 문서 언어는 한국어를 기본으로 한다.
- 모든 단계 스펙 문서(`docs/phases/*.md`)는 도구 중립적으로 작성한다 (Claude Code 서브에이전트 문법이나 Codex 전용 문법에 종속되지 않는 순수 지침형 마크다운).
- `.claude/agents/*.md`는 Claude Code 서브에이전트 정의. Codex에서는 같은 역할을 `docs/phases/*.md`의 지침을 사람이 프롬프트로 넘겨 재현한다.
- `.claude/skills/*/SKILL.md`는 단계 진입점(슬래시 커맨드)이며, 실제 작업 지침은 `docs/phases/*.md`를 참조하도록 얇게 유지한다.
- 실제 소설 프로젝트는 `projects/<slug>/`에 생성하며, `projects/_TEMPLATE/`을 복제해서 시작한다.
